[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "robot-fleet-client-sdk"
version = "0.0.0"
description = "Python client SDK for robot-fleet gateway"
requires-python = ">=3.11"
dependencies = ["httpx", "websockets"]

